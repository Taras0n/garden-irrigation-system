import styles from "../Header/Header.module.scss"

const Header = () => {
    return ( 
        <header className={styles.header}>
            <h1>System nawadniania ogrodu</h1>
        </header>
    );
};

export default Header;